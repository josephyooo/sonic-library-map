services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.next
    ports:
      - "3000:3000"
    environment:
      UMAP_SERVICE_URL: http://umap:8000
      DATABASE_PATH: /data/spotify-library.db
    env_file:
      - .env.local
    volumes:
      - db-data:/data
    depends_on:
      umap:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "-T", "3", "http://localhost:3000/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

  umap:
    build:
      context: .
      dockerfile: Dockerfile.python
    expose:
      - "8000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      retries: 3

volumes:
  db-data:
